from:
<input
  min={{this.min1}}
  max={{this.max1}}
  type="number"
  {{on "input" (fn this.addMinimumRange)}}
/>
to:
<input
  min={{this.min2}}
  max={{this.max2}}
  type="number"
  {{on "input" (fn this.addMaximumRange)}}
/>
