<thead>
  <tr>
    {{#each @headerData as |headerCell|}}
      <HeaderCell
        @headingName={{headerCell}}
        @sortBy={{this.sortBy}}
        @sorter={{this.sorter}}
      />
    {{/each}}
  </tr>
</thead>
