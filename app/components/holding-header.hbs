<thead class="shadow">
  <tr>
    {{#each @headerData as |headerCell|}}
      <HeaderCell
        @headingName={{headerCell}}
        @sortBy={{@sortBy}}
        @sorter={{@sorter}}
      />
    {{/each}}
  </tr>
</thead>
